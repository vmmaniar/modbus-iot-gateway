# Generated by KiCad's plot + fabrication-output menus, or via `kicad-cli`
# in the kicad-checks GitHub Actions workflow.
#
# Expected contents after Phases 4–5 complete:
#   schematic.pdf              — full schematic (4 pages)
#   board_top.pdf, board_bottom.pdf  — assembly drawings
#   gerbers.zip                — fab files (8 layers + drill)
#   pick_and_place.csv         — centroid file
#   bom.csv                    — components + supplier P/Ns
#   drc_report.txt, erc_report.txt   — zero violations expected
