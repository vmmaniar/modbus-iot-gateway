# Pinned to keep Phase 0 reproducible.
# pymodbus 3.x had multiple breaking API changes; this version is verified to work
# with the firmware-twin in simulator/firmware_twin.py.
pymodbus==3.13.0
pyserial>=3.5
crcmod>=1.7
cbor2>=5.6
pytest>=8.0
